{
  "name": "rich-text-editor-paginated",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:wasm": "cd engine && wasm-pack build --target web --out-dir ../src/lib/engine-wasm",
    "build:all": "npm run build:wasm && npm run build"
  },
  "dependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "@tsconfig/svelte": "^5.0.6",
    "svelte": "^5.46.1",
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  }
}
